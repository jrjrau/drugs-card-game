{
  "name": "drugs-card-game-server",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node test/rules.test.js && node test/share-link.test.js",
    "build": "esbuild sdk-entry.js --bundle --minify --format=iife --global-name=DiscordEmbedded --outfile=public/vendor/discord-sdk.js"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
